//Stuff to review for practicum...

//for 2d array, does columns or rows have to be constant?
//columns have to be constant, can add more rows, but if adding columns you would need to start over.
//int row_total(int table[], )
//
//what code is needed to fill an array int values[5], with 1,4,9,16,25
int values[5];
for(int i = 0; i < 5; i++)
{
    values[i] = (i + 1) * (i + 1);
}
//fill array with 0,1,2,1,0
int values[5];
for(int i = 0; i < 5; i++)
{
    if(i < 3)
    {
        values[i] = i;
    }
    else
    {
        values[i] = 
    }
}


int total = 0;
for(int i = 0; i < 6; i++)
{
    
    total = total + a[i];
}
//adds them up.

//
int total = 0;
for(int i = 0; i < 6; i = i + 2)
{
    
    total = total + a[i];
}
==5...

//
//

int tfunc1(int a)
{
    int x = a * 2;
    return x;
}

void func2(int a)
{
    int x = a * 2;
    cout << x;
}

//both would return 10; with no endl == 1010
//
if(idx < 0 || idx > size -1) //below zero, above size
opposite of 
if(idx >= 0 && idx < size) //between points in first one, above zero, below size\
//
//what is output?
int variable
cout << variable;
//output=
//garbage
//array is preinitialized but not ints.

//what is output?
string variable;
cout << variable
//output is empty string.

//what is result
string firstname = "mac";
string lastname = "hines";
cout << firstname + lastname;
//result = machines, no endl;

//what is string length code.
string name = "Rumpelstilskin"
int number = name.length();
cout << number;
//outputs length

//what is output
string word = "individual";
string piece = word.substr(6);
cout << piece;
//output is dual
//takes position 6 to the end.
//substr with one input is from there to the end
//substr with two inputs is first postion and the length.

//what is output


